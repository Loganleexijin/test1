# 📱 Flux 断食APP - 迭代三：个人中心重构

**版本**：v1.3
**模块名称**：身心能量仪表盘 (Body & Mind Dashboard) & 勋章墙 (Badge Wall)
**功能定位**：将个人中心从简单的数据展示升级为情感化的成就可视化中心
**设计目标**：通过可视化和游戏化机制，将断食数据转化为具有情感价值的"身心红利"，提升用户长期留存

---

## 1. 模块定位与页面架构

### 1.1 位置与入口

- **主入口**：底栏 Tab 3（个人中心图标）
- **替代对象**：原有的简易"个人中心"页面
- **页面层级**：顶级导航页（与首页、历史记录平级）

### 1.2 页面结构

```jsx
个人中心页面 (Profile Screen)
├─ 顶部：用户信息卡片（头像、昵称、个人ID、会员状态）
├─ 核心区：身心能量仪表盘
│   ├─ 代谢年龄展示区
│   └─ 四维数据卡片矩阵 (2x2)
├─ 成就区：灵动勋章墙
│   └─ 动态勋章网格（3-4列自适应）
├─ 设置区：通用设置模块
│   ├─ 个人信息设置（头像、昵称、个人ID）
│   ├─ 账号与安全（手机号、登录密码）
│   ├─ 消息通知设置
│   └─ 法律与合规（服务条款、隐私政策等）
└─ 底部：退出登录 & 删除账号
```

---

## 2. 交互设计风格定义

> **关键原则**：延续 Flux 的"有机 (Organic) + 空灵 (Ethereal)"核心气质，但在个人中心增强"成就感"与"沉浸感"。
> 

### 2.1 视觉语言

| 设计元素 | 具体实现 | 设计意图 |
| --- | --- | --- |
| **玻璃拟态** | 使用 `backdrop-blur-lg` + 半透明背景 | 营造轻盈、未来感 |
| **渐变色系统** | 每个数据维度使用专属渐变色（见下文） | 强化信息层级与记忆点 |
| **微动效** | 卡片 hover 时轻微上浮 (translateY -2px) + 阴影加深 | 增强可点击性暗示 |
| **呼吸光效** | 关键数据（如代谢年龄）使用缓慢的 scale 动画 (1.0 → 1.02) | 吸引注意力，营造"活力"感 |
| **粒子装饰** | 勋章解锁时的金色粒子飘散效果 | 强化仪式感与成就感 |

### 2.2 动效时长标准

- **页面进入**：500ms (ease-out)
- **卡片交互**：200ms (ease-in-out)
- **勋章解锁动画**：1200ms (包含翻转 + 光效 + 音效)
- **数据更新**：300ms (数字滚动效果)

### 2.3 间距与圆角规范

- **卡片圆角**：`rounded-2xl` (16px)
- **勋章圆角**：`rounded-full` (完全圆形)
- **卡片间距**：垂直 `space-y-4` (16px)，水平 `gap-3` (12px)
- **页面边距**：`px-5` (20px)

---

## 3. 身心能量仪表盘 (Body & Mind Dashboard)

### 3.1 顶部核心指标：代谢年龄 (Metabolic Age)

### 布局设计

```
┌─────────────────────────────────────────┐
│  🌱 生命之树图标 (动态生长动画)           │
│                                         │
│  实际年龄              代谢年龄          │
│    32                  29.5            │
│  ────────────────────────────            │
│                                         │
│  您的代谢状态年轻了 2.5 岁 ✨            │
└─────────────────────────────────────────┘
```

### 视觉规范

- **图标选择**：`Sprout` (Lucide Icons，象征生长与活力)
- **配色方案**：
    - 实际年龄：中性灰 `text-gray-500`
    - 代谢年龄：渐变金色 `from-yellow-400 to-amber-500` (高亮显示)
- **背景渐变**：`from-emerald-50 to-teal-50` (清新绿)
- **动画效果**：图标使用 `animate-pulse` (缓慢呼吸感)

### 计算逻辑

```jsx
// 代谢年龄计算公式 (MVP 简化版)
const metabolicAge = Math.max(
  actualAge - (totalFastingDays * 0.05) - (weightLoss * 0.5),
  actualAge - 5  // 最多年轻 5 岁（避免数据失真）
);
```

### 交互行为

- **点击卡片**：弹出 Modal，展示详细计算依据（累计断食天数、减重公斤数等）
- **长按卡片**：触发分享功能（生成带有数据的精美卡片图）

---

### 3.2 四维数据卡片矩阵

### 整体布局

使用 `grid grid-cols-2 gap-3`，确保在不同屏幕尺寸下自适应。

---

### A. 🧬 细胞净化 (Autophagy Tracker)

**数据定义**

- 统计用户处于"深度自噬状态"的总时长
- **算法**：单次断食时长中，**超过 16 小时**的部分计入
    - 例：断食 18 小时 = 2 小时自噬时长

**UI 元素**

- **图标**：`Sparkles` (Lucide)
- **配色**：`from-indigo-500 to-purple-600` (蓝紫色渐变)
- **文案结构**：
    
    ```
    ✨ 细胞净化
    
    12 小时
    本周累计
    
    相当于为身体进行了
    一次深度大扫除
    ```
    

**交互反馈**

- **点击**：展开"什么是细胞自噬？"科普卡片（引用 2016 年诺贝尔奖）
- **数据更新时**：数字以翻页动画滚动变化

---

### B. 🛡️ 胰脏休假 (Insulin Rest)

**数据定义**

- 统计用户处于"非进食窗口"的总时长
- **算法**：累计所有断食时长（包括未完成目标的记录）

**UI 元素**

- **图标**：`Shield` (Lucide)
- **配色**：`from-emerald-400 to-teal-500` (青绿色渐变)
- **文案结构**：
    
    ```
    🛡️ 胰脏休假
    
    200 小时
    本月累计
    
    您的胰脏获得了宝贵
    的休假，代谢灵活性提升
    ```
    

**数据来源**

- 从 `AsyncStorage` 中的 `totalFastingHours` 读取

---

### C. 💰 财富积累 (Money Saved)

**数据定义**

- 将少吃的饭量化为金钱激励
- **算法**：`完成的断食次数 × 单餐价格`

**UI 元素**

- **图标**：`PiggyBank` (Lucide)
- **配色**：`from-orange-400 to-amber-500` (金橙色渐变)
- **文案结构**：
    
    ```
    💰 财富积累
    
    ¥ 450
    已节省
    
    这笔钱够买一双新跑鞋
    或奖励一次按摩
    ```
    

**交互设计**

- **长按卡片**：进入"单餐价格设置"页面（默认 ¥30）
- **动画**：金额数字以金币掉落音效 + 滚动数字形式呈现

---

### D. 🔥 热量与减重 (Weight Loss)

**数据定义**

- **算法**：`初始体重 - 当前体重`
- 需要用户在设置中输入初始体重和当前体重

**UI 元素**

- **图标**：`TrendingDown` (Lucide)
- **配色**：`from-rose-400 to-pink-500` (红粉色渐变)
- **文案结构**：
    
    ```
    🔥 热量与减重
    
    3.5 kg
    已减轻
    
    您的身体负担
    正在减轻
    ```
    

**数据来源**

- 从用户设置的 `initialWeight` 和 `currentWeight` 计算

---

## 4. 灵动勋章墙 (Badge Wall)

### 4.1 设计原则

**零图片素材策略 (No Image Assets)**

- 完全使用 CSS (`LinearGradient`) + 图标 (`Lucide Icons`) 绘制
- 优势：体积小、风格统一、易于维护

### 4.2 视觉状态设计

### 未解锁状态

```css
/* 视觉效果 */
- 灰度滤镜：filter: grayscale(100%)
- 透明度：opacity: 0.4
- 锁定图标：Lock (Lucide) 覆盖在中心
- 进度提示：底部显示"还需坚持 X 天"
```

### 已解锁状态

```css
/* 视觉效果 */
- 玻璃拟态：backdrop-blur-md + 半透明白色背景
- 外发光：box-shadow: 0 0 20px rgba(color, 0.6)
- 图标动画：缓慢旋转 (rotate 0deg → 360deg, 10s infinite)
- 渐变边框：border-image: linear-gradient(...)
```

### 4.3 交互行为

| 操作 | 未解锁勋章 | 已解锁勋章 |
| --- | --- | --- |
| **点击** | 弹出 Toast："还需坚持 X 天解锁" | 卡片翻转动画，背面显示获得日期 + 祝贺语 |
| **长按** | 显示解锁条件详情 | 触发分享功能（生成勋章卡片图） |
| **首次解锁** | - | 全屏庆祝动画（金色粒子 + 音效 + 震动反馈） |

### 4.4 翻转动画技术实现

```jsx
// React Native Animated API
const flipAnimation = () => {
  Animated.sequence([
    Animated.timing(flipValue, {
      toValue: 90,
      duration: 300,
      easing: Easing.ease,
    }),
    Animated.timing(flipValue, {
      toValue: 0,
      duration: 300,
      easing: Easing.ease,
    }),
  ]).start();
};
```

---

### 4.5 勋章内容规划 (MVP 首发版)

| 勋章名称 | 触发条件 | 图标 | 配色方案 | 祝贺语 |
| --- | --- | --- | --- | --- |
| **初次觉醒** | 完成第 1 次断食 | `Sunrise` | Orange → Yellow | 欢迎来到灵动断食的世界！你已迈出改变的第一步。 |
| **燃脂大师** | 单次断食 > 18h | `Flame` | Red → Purple | 您已触达深度燃脂区，脂肪正在燃烧！ |
| **自噬专家** | 累计自噬时长 > 50h | `Dna` | Blue → Cyan | 您的细胞正在焕发新生，这是真正的抗衰老。 |
| **七日连胜** | 连续打卡 7 天 | `CalendarCheck` | Gold → White | 坚持就是胜利，习惯已经养成！ |
| **省钱小能手** | 累计省钱 > ¥500 | `Wallet` | Green → Emerald | 健康与财富双丰收，给自己买个礼物吧！ |
| **AI 探索者** | 使用 AI 拍照 > 10 次 | `ScanLine` | Purple → Pink | 您已掌握了科技断食的奥秘。 |

---

## 5. 通用设置模块 (Settings Module)

### 5.1 模块定位与布局

**位置**：位于勋章墙下方，作为个人中心页面的底部板块

**设计原则**：采用分组卡片设计，每个功能组使用独立卡片，保持视觉层级清晰

---

### 5.2 个人信息设置 (Personal Info)

### 5.2.1 功能入口设计

**卡片标题**：个人信息

**图标**：`User` (Lucide)

**布局**：垂直列表，每项包含左侧图标 + 标签 + 右侧内容/箭头

### 5.2.2 功能项详情

### A. 头像设置

**展示形式**

```jsx
[头像圆形图] 头像                    [>]
```

**交互流程**

1. 点击进入「头像设置」页面
2. 展示当前头像（大图预览）
3. 提供三个操作按钮：
    - **拍照**：调用相机
    - **从相册选择**：调用系统相册
    - **删除头像**（仅当已设置头像时显示）
4. 选择/拍照后进入裁剪页面（1:1 方形裁剪）
5. 确认后上传至服务器，更新头像

**技术实现**

- 使用 `expo-image-picker` 调用相机/相册
- 使用 `react-native-image-crop-picker` 实现裁剪
- 头像存储：上传至云存储（阿里云OSS/AWS S3），URL 保存到用户数据库
- 本地缓存：使用 `AsyncStorage` 缓存头像 URL

**默认头像**

- 未设置头像时，显示用户昵称首字符（大写）
- 背景色根据用户ID生成（确保一致性）

---

### B. 昵称设置

**展示形式**

```jsx
[用户图标] 昵称      当前昵称           [>]
```

**交互流程**

1. 点击进入「昵称设置」页面
2. 显示输入框，预填当前昵称
3. 输入新昵称（实时字数统计）
4. 点击「保存」按钮更新

**验证规则**

- 长度：2-20 个字符（中文算 1 个字符）
- 允许字符：中文、英文、数字、下划线
- 禁止：纯数字、纯符号、敏感词
- 冷却时间：30 天内最多修改 2 次（防止频繁修改）

**视觉设计**

- 输入框：`rounded-xl` 圆角，浅色背景
- 字数提示：右下角显示「12/20」
- 保存按钮：固定在页面底部，禁用状态为灰色

**错误提示**

- 昵称过短/过长：「昵称长度为 2-20 个字符」
- 包含非法字符：「昵称仅支持中英文、数字和下划线」
- 修改次数超限：「昵称 30 天内最多修改 2 次，下次可修改时间：2026-02-10」

---

### C. 个人ID（只读）

**展示形式**

```jsx
[标签图标] 个人ID     FLUX_123456        [复制]
```

**功能说明**

- **生成规则**：`FLUX_` + 6位随机数字/字母（注册时自动生成，永久不变）
- **用途**：用于用户间添加好友、客服查询等（未来功能预留）
- **交互**：点击右侧「复制」按钮，复制到剪贴板，并显示 Toast：「个人ID已复制」

**视觉设计**

- 个人ID 使用等宽字体 `font-mono`
- 「复制」按钮使用浅色背景 + 小字号

---

### 5.3 账号与安全 (Account & Security)

### 5.3.1 功能入口设计

**卡片标题**：账号与安全

**图标**：`ShieldCheck` (Lucide)

**布局**：垂直列表

### 5.3.2 功能项详情

### A. 手机号

**展示形式**

```jsx
[手机图标] 手机号     138****8888        [>]
```

**交互流程（绑定/更换）**

1. 点击进入「手机号设置」页面
2. **未绑定时**：
    - 显示「绑定手机号」引导文案
    - 输入手机号 + 获取验证码按钮
    - 输入验证码后绑定
3. **已绑定时**：
    - 显示「更换手机号」选项
    - 流程：验证原手机号（短信验证码）→ 输入新手机号 → 验证新手机号

**验证码规则**

- 60秒倒计时，期间按钮禁用
- 验证码 6 位数字，10 分钟有效
- 单日单手机号最多发送 5 次（防刷）

**安全提示**

- 页面顶部显示：「手机号用于登录和找回密码，请确保手机号真实有效」
- 更换手机号时额外提示：「更换手机号后，原手机号将无法登录」

---

### B. 登录密码

**展示形式**

```jsx
[锁图标] 登录密码     已设置              [>]
```

**交互流程（设置/修改）**

1. 点击进入「登录密码设置」页面
2. **未设置密码时**：
    - 输入新密码（密码强度实时检测）
    - 再次输入确认密码
    - 点击「完成」设置密码
3. **已设置密码时**：
    - 输入原密码（带「忘记密码？」链接）
    - 输入新密码
    - 再次输入确认密码
    - 点击「完成」更新密码

**密码规则**

- 长度：8-20 位
- 必须包含：字母 + 数字
- 建议包含：大小写字母 + 特殊符号
- 禁止：纯数字、纯字母、常见弱密码（123456、password等）

**密码强度指示器**

```jsx
┌────────────────────────────────┐
│ 新密码    ●●●●●●●●               │
│                                │
│ 密码强度  [■■■□□] 中等         │
└────────────────────────────────┘
```

- 弱：红色（纯数字/纯字母，<8位）
- 中：橙色（字母+数字，8-12位）
- 强：绿色（字母+数字+符号，>12位）

**忘记密码流程**

1. 点击「忘记密码？」
2. 验证手机号（发送验证码）
3. 输入新密码
4. 完成重置

---

### 5.4 消息通知设置 (Notification Settings)

### 5.4.1 功能入口设计

**卡片标题**：消息通知

**图标**：`Bell` (Lucide)

**布局**：垂直列表，每项右侧为 Toggle 开关

### 5.4.2 通知项详情

| 通知项 | 默认状态 | 说明 |
| --- | --- | --- |
| **断食提醒** | 开启 | 断食结束时推送通知（需要通知权限） |
| **进食窗口提醒** | 关闭 | 进食窗口即将结束时提醒（提前15分钟） |
| **每日打卡提醒** | 开启 | 每天固定时间提醒开始断食（用户可设置时间） |
| **勋章解锁提醒** | 开启 | 解锁新勋章时推送通知 |
| **成就分享提醒** | 关闭 | 达成新里程碑时提示分享（非推送，仅应用内） |
| **系统通知** | 开启 | 重要更新、公告等（不可关闭） |

### 5.4.3 权限引导

**未授予通知权限时的处理**

- 在通知设置页面顶部显示横幅：
    
    ```
    ⚠️ 通知权限未开启
    部分提醒功能无法使用，请前往系统设置开启
    [前往设置]
    ```
    
- 点击「前往设置」调用 `Linking.openSettings()` 打开系统设置

### 5.4.4 时间设置（针对每日打卡提醒）

**展示形式**

```jsx
[时钟图标] 每日打卡提醒    [开关]
              提醒时间      20:00    [>]
```

**交互**

- 点击「提醒时间」进入时间选择器（滚轮式选择器）
- 默认时间：20:00（晚上 8 点）
- 保存后，每天在该时间推送通知

---

### 5.5 法律与合规 (Legal & Compliance)

### 5.5.1 功能入口设计

**卡片标题**：法律与合规

**图标**：`FileText` (Lucide)

**布局**：垂直列表

### 5.5.2 功能项详情

### A. 服务条款

**展示形式**

```jsx
[文档图标] 服务条款                    [>]
```

**交互**

- 点击进入「服务条款」页面
- 显示完整的用户服务协议（可滚动的长文本）
- 底部显示最后更新日期：「最后更新：2026-01-01」

**内容结构（示例大纲）**

1. 协议接受与变更
2. 服务内容与使用规范
3. 用户权利与义务
4. 知识产权声明
5. 免责条款（特别是健康建议免责）
6. 争议解决

---

### B. 隐私政策

**展示形式**

```jsx
[盾牌图标] 隐私政策                    [>]
```

**交互**

- 点击进入「隐私政策」页面
- 显示完整的隐私保护政策
- 底部显示最后更新日期

**内容结构（示例大纲）**

1. 信息收集范围
2. 信息使用方式
3. 信息存储与保护
4. 第三方服务（如 RevenueCat、云存储）
5. 用户权利（查看、删除、导出数据）
6. Cookie 与追踪技术
7. 政策更新通知

---

### C. 个人信息收集清单

**展示形式**

```jsx
[列表图标] 个人信息收集清单            [>]
```

**交互**

- 点击进入「个人信息收集清单」页面
- 以表格/卡片形式展示所有收集的数据

**内容结构**

| 信息类型 | 收集内容 | 用途 | 是否必需 |
| --- | --- | --- | --- |
| **账号信息** | 手机号、密码、昵称、头像 | 账号识别、登录验证 | 必需 |
| **健康数据** | 体重、身高、年龄、断食记录 | 功能实现、数据统计 | 必需 |
| **设备信息** | 设备型号、操作系统版本、APP版本 | 问题排查、兼容性优化 | 必需 |
| **相机/相册** | 食物照片（仅限AI分析使用） | AI 餐盘分析 | 可选 |
| **通知权限** | 推送Token | 发送断食提醒 | 可选 |
| **使用行为** | 页面访问、功能使用频次 | 产品优化、功能改进 | 可选（可关闭） |

**底部说明**

- 「我们不会收集您的通讯录、位置信息、剪贴板内容」
- 「所有健康数据仅存储在您的设备和我们的加密服务器，不会与第三方共享」

---

### 5.6 账号操作 (Account Actions)

### 5.6.1 退出登录

**展示形式**

```jsx
[退出图标] 退出登录                    [>]
```

**交互流程**

1. 点击后弹出确认弹窗（Modal）：
    
    ```
    确认退出登录？
    
    退出后您的数据仍会保留在云端，
    下次登录可继续使用。
    
    [取消]  [确认退出]
    ```
    
2. 点击「确认退出」后：
    - 清除本地 Token（`AsyncStorage`）
    - 保留用户数据（断食记录、勋章等不删除）
    - 跳转到登录页

**视觉设计**

- 「退出登录」按钮使用中性色（不是危险色）
- 确认弹窗的「确认退出」按钮使用橙色（警示但不恐吓）

---

### 5.6.2 删除账号

**展示形式**

```jsx
[垃圾桶图标] 删除账号                  [>]
```

**交互流程（多重确认）**

**第一步：警告页面**

- 点击后进入独立的「删除账号」页面
- 顶部红色警告框：
    
    ```
    ⚠️ 重要提示
    
    删除账号后，以下数据将永久删除且无法恢复：
    • 所有断食记录和历史数据
    • 已解锁的勋章和成就
    • 会员权益（若有）
    • 个人资料和设置
    
    如果您只是想暂停使用，建议选择「退出登录」。
    ```
    

**第二步：验证身份**

- 滑动到页面底部，显示「继续删除」按钮
- 点击后要求验证身份：
    - 方式1：输入登录密码
    - 方式2：手机验证码（若未设置密码）

**第三步：最终确认**

- 验证通过后弹出最终确认弹窗：
    
    ```
    最后确认
    
    请输入「删除账号」以确认操作
    
    [输入框]
    
    [取消]  [确认删除]
    ```
    
- 用户必须手动输入「删除账号」四个字（区分大小写）
- 输入匹配后「确认删除」按钮才可点击

**第四步：执行删除**

- 点击「确认删除」后：
    - 显示加载动画：「正在删除您的账号...」
    - 调用后端 API 删除用户数据（符合 GDPR 要求）
    - 删除成功后跳转到欢迎页/登录页
    - 显示 Toast：「账号已删除」

**视觉设计**

- 「删除账号」按钮使用红色（`text-red-600`）
- 警告框使用红色背景 + 白色文字
- 整个流程强调不可逆性，但不过度恐吓

**后端实现要点**

- 软删除：标记账号为已删除，保留数据 30 天（冷静期，用户可联系客服恢复）
- 硬删除：30 天后自动执行物理删除
- 删除日志：记录删除操作的时间和 IP（合规要求）

---

## 6. 技术实施规范

### 6.1 数据存储扩展 (AsyncStorage Schema)

```jsx
const userStats = {
  // 原有字段（迭代一已定义）
  totalFastingHours: 0,
  fastingStreak: 0,
  
  // 新增字段（迭代三）
  deepAutophagyHours: 0,      // 细胞净化时长（>16h 的部分累加）
  moneySaved: 0,              // 财富积累（自动计算）
  mealCostSetting: 30,        // 单餐价格设置（用户可编辑）
  initialWeight: null,        // 初始体重
  currentWeight: null,        // 当前体重
  actualAge: null,            // 实际年龄（用于代谢年龄计算）
  
  badgesUnlocked: [],         // 已解锁勋章 ID 列表
                              // 例如：['badge_first_fast', 'badge_fat_burning']
};
```

### 6.2 勋章解锁检测逻辑

```jsx
// 每次断食完成后调用此函数
function checkBadgeUnlock(userStats) {
  const newBadges = [];
  
  // 检测逻辑示例
  if (userStats.totalFastingHours >= 1 && 
      !userStats.badgesUnlocked.includes('badge_first_fast')) {
    newBadges.push('badge_first_fast');
  }
  
  if (userStats.longestFastingHours >= 18 && 
      !userStats.badgesUnlocked.includes('badge_fat_burning')) {
    newBadges.push('badge_fat_burning');
  }
  
  // ... 其他勋章检测
  
  return newBadges; // 返回新解锁的勋章列表
}
```

### 6.3 完整 UI 组件结构

```jsx
ProfileScreen
├─ ScrollView (整体可滚动)
│   ├─ UserInfoCard (用户信息卡片)
│   │   ├─ Avatar (头像)
│   │   ├─ Nickname (昵称)
│   │   └─ UserID (个人ID + 复制按钮)
│   ├─ MetabolicAgeCard (代谢年龄卡片)
│   ├─ DataGrid (2x2 数据卡片网格)
│   │   ├─ DataCard (细胞净化)
│   │   ├─ DataCard (胰脏休假)
│   │   ├─ DataCard (财富积累)
│   │   └─ DataCard (热量与减重)
│   ├─ BadgeWall (勋章墙)
│   │   └─ BadgeItem × N (单个勋章组件)
│   ├─ SettingsSection (通用设置)
│   │   ├─ PersonalInfoCard (个人信息)
│   │   │   ├─ AvatarSetting (头像设置)
│   │   │   ├─ NicknameSetting (昵称设置)
│   │   │   └─ UserIDDisplay (个人ID)
│   │   ├─ AccountSecurityCard (账号与安全)
│   │   │   ├─ PhoneSetting (手机号)
│   │   │   └─ PasswordSetting (登录密码)
│   │   ├─ NotificationCard (消息通知)
│   │   │   ├─ FastingReminder (断食提醒)
│   │   │   ├─ EatingWindowReminder (进食窗口提醒)
│   │   │   ├─ DailyCheckInReminder (每日打卡提醒)
│   │   │   └─ ... (其他通知项)
│   │   └─ LegalCard (法律与合规)
│   │       ├─ TermsOfService (服务条款)
│   │       ├─ PrivacyPolicy (隐私政策)
│   │       └─ DataCollectionList (个人信息收集清单)
│   └─ AccountActionsSection (账号操作)
│       ├─ LogoutButton (退出登录)
│       └─ DeleteAccountButton (删除账号)
```

### 6.4 用户数据结构扩展

```jsx
const userProfile = {
  // 基础信息
  userId: 'FLUX_123456',           // 个人ID（注册时生成，不可变）
  nickname: '用户昵称',             // 昵称（可修改，有冷却期）
  avatar: 'https://...jpg',        // 头像URL
  phone: '138****8888',            // 手机号（脱敏显示）
  phoneVerified: true,             // 手机号是否已验证
  hasPassword: true,               // 是否已设置密码
  
  // 会员信息
  isPro: false,                    // 是否为Pro会员
  proExpireAt: null,               // Pro到期时间
  
  // 通知设置
  notificationSettings: {
    fastingReminder: true,         // 断食提醒
    eatingWindowReminder: false,   // 进食窗口提醒
    dailyCheckIn: true,            // 每日打卡提醒
    dailyCheckInTime: '20:00',     // 打卡提醒时间
    badgeUnlock: true,             // 勋章解锁提醒
    achievementShare: false,       // 成就分享提醒
    systemNotification: true,      // 系统通知（不可关闭）
  },
  
  // 权限状态
  permissions: {
    notification: true,            // 通知权限
    camera: true,                  // 相机权限
    photoLibrary: true,            // 相册权限
  },
  
  // 账号元数据
  createdAt: '2026-01-01T00:00:00Z',  // 注册时间
  lastLoginAt: '2026-01-10T08:00:00Z',  // 最后登录时间
  nicknameChangedCount: 0,       // 昵称修改次数（30天内）
  nicknameLastChangedAt: null,   // 昵称最后修改时间
};
```

### 6.5 样式实现建议 (NativeWind)

```jsx
// 数据卡片基础样式
<View className="bg-white/80 backdrop-blur-lg rounded-2xl p-4 shadow-lg">
  <LinearGradient
    colors={['#6366f1', '#9333ea']} // 示例：蓝紫色渐变
    className="absolute inset-0 rounded-2xl opacity-10"
  />
  {/* 卡片内容 */}
</View>

// 勋章组件基础样式（已解锁）
<View className="relative w-20 h-20 rounded-full items-center justify-center">
  <LinearGradient
    colors={['#f59e0b', '#eab308']} // 示例：金色渐变
    className="absolute inset-0 rounded-full"
  />
  <View className="bg-white/20 backdrop-blur-md rounded-full p-3">
    <Icon name="Flame" size={32} color="white" />
  </View>
</View>
```

### 6.6 设置页面的通用样式

```jsx
// 设置卡片（整体容器）
<View className="bg-white rounded-2xl p-4 mb-4 shadow-sm">
  <Text className="text-lg font-semibold mb-3">卡片标题</Text>
  {/* 列表项 */}
</View>

// 设置列表项（可点击）
<TouchableOpacity 
  className="flex-row items-center py-3 border-b border-gray-100"
  activeOpacity={0.7}
>
  <Icon name="IconName" size={20} color="#6b7280" />
  <Text className="flex-1 ml-3 text-base">功能名称</Text>
  <Text className="text-gray-400 mr-2">当前值</Text>
  <Icon name="ChevronRight" size={16} color="#d1d5db" />
</TouchableOpacity>

// Toggle 开关项
<View className="flex-row items-center py-3 border-b border-gray-100">
  <Icon name="IconName" size={20} color="#6b7280" />
  <View className="flex-1 ml-3">
    <Text className="text-base">功能名称</Text>
    <Text className="text-xs text-gray-400 mt-1">功能说明</Text>
  </View>
  <Switch
    value={isEnabled}
    onValueChange={toggleSwitch}
    trackColor= false: '#e5e7eb', true: '#10b981' 
    thumbColor={isEnabled ? '#ffffff' : '#f9fafb'}
  />
</View>

// 危险操作按钮（删除账号）
<TouchableOpacity 
  className="bg-red-50 rounded-xl p-4 items-center"
  activeOpacity={0.7}
>
  <Text className="text-red-600 font-medium">删除账号</Text>
</TouchableOpacity>
```

---

## 7. 给 Cursor 的完整执行指令

```markdown
Role: Senior React Native Developer with expertise in user authentication and settings UI

Task: Implement the complete Profile Screen including Dashboard, Badge Wall, and Settings Module.

Phase 1: Data Structure
1. Extend AsyncStorage schema:
   - userProfile: userId, nickname, avatar, phone, hasPassword, etc.
   - notificationSettings: object with all notification preferences
   - permissions: notification, camera, photoLibrary status

2. Implement helper functions:
   - generateUserId() → returns "FLUX_" + 6-char random string
   - validateNickname(string) → validates length, chars, cooldown
   - validatePassword(string) → checks strength, returns level (weak/medium/strong)
   - checkNicknameChangeCooldown(lastChangeDate) → returns canChange and daysLeft

Phase 2: UI Components - Settings Module
1. PersonalInfoCard
   - AvatarSetting: tap to open image picker → crop (1:1) → upload
   - NicknameSetting: tap to open input modal with validation
   - UserIDDisplay: display FLUX_XXXXXX with copy button

2. AccountSecurityCard
   - PhoneSetting: show masked phone, tap to bind/change with SMS verification
   - PasswordSetting: show "已设置" status, tap to change with strength indicator

3. NotificationCard
   - Render list of Toggle switches for each notification type
   - Show permission warning banner if notification permission not granted
   - DailyCheckInReminder includes time picker (default 20:00)

4. LegalCard
   - Three items: TermsOfService, PrivacyPolicy, DataCollectionList
   - Each opens a full-screen scrollable document page
   - Show "最后更新：YYYY-MM-DD" at bottom

5. AccountActionsSection
   - LogoutButton: show confirmation modal with neutral color
   - DeleteAccountButton: multi-step flow (warning → verification → type "删除账号" → execute)

Phase 3: Styling Requirements
- Use card-based layout with rounded-2xl
- Each setting item has icon (left) + label (center) + value/chevron (right)
- Use gray-100 for dividers between items
- Toggle switches use green (10b981) for active state
- Delete account uses red-50 background with red-600 text
- All interactive elements use activeOpacity={0.7}

Phase 4: Security & Validation
- SMS verification: 60s cooldown, 6-digit code, max 5 attempts per day
- Password: 8-20 chars, must include letters + numbers
- Nickname: 2-20 chars, only Chinese/English/numbers/underscore, max 2 changes per 30 days
- Account deletion: requires password OR SMS code, then type "删除账号" to confirm

Phase 5: Navigation
- Each setting item navigates to a dedicated screen
- Use React Navigation Stack for sub-pages
- Implement back button in header for all sub-pages

Please start by:
1. Defining all TypeScript interfaces for user data
2. Creating the PersonalInfoCard component with avatar picker
3. Implementing the NotificationCard with toggles
```

---

## 8. 验收标准 (MVP Acceptance Criteria)

### 8.1 数据展示模块

- [ ]  页面可通过底栏 Tab 3 访问
- [ ]  用户信息卡片正确显示头像、昵称、个人ID
- [ ]  代谢年龄卡片正确显示计算结果，且有呼吸动画
- [ ]  四个数据卡片均显示正确数据，配色符合规范
- [ ]  至少有 3 个勋章可解锁（首次断食、七日连胜、省钱小能手）
- [ ]  勋章未解锁时显示灰色 + 锁定图标
- [ ]  勋章解锁时触发翻转动画和庆祝效果
- [ ]  所有交互响应时间 < 300ms
- [ ]  数据持久化（刷新后不丢失）

### 8.2 个人信息设置模块

- [ ]  头像设置：可拍照、选择相册、裁剪（1:1）、上传成功
- [ ]  昵称设置：输入验证正确、字数统计准确、冷却期限制生效
- [ ]  个人ID：正确显示、点击复制成功并提示
- [ ]  未设置头像时显示昵称首字符作为默认头像

### 8.3 账号与安全模块

- [ ]  手机号：可绑定、可更换、验证码倒计时正常、脱敏显示
- [ ]  登录密码：密码强度实时检测、确认密码一致性验证、旧密码验证
- [ ]  忘记密码流程可正常完成
- [ ]  验证码单日发送次数限制生效

### 8.4 消息通知模块

- [ ]  所有通知开关可正常切换
- [ ]  未授予通知权限时显示警告横幅
- [ ]  「前往设置」按钮可正确跳转到系统设置
- [ ]  每日打卡提醒时间可正常设置（滚轮选择器）
- [ ]  保存后在设定时间推送通知

### 8.5 法律与合规模块

- [ ]  服务条款、隐私政策、个人信息收集清单可正常访问
- [ ]  文档内容可滚动、显示完整
- [ ]  底部显示最后更新日期
- [ ]  个人信息收集清单以表格形式清晰展示

### 8.6 账号操作模块

- [ ]  退出登录：确认弹窗显示、确认后清除Token、跳转到登录页
- [ ]  删除账号：警告页面显示、身份验证成功、手动输入确认文字匹配、删除成功并跳转
- [ ]  删除账号流程中任意步骤点击「取消」可正常返回
- [ ]  删除账号后数据确实被删除（后端验证）